class Project{
   static int gcd(int x,int y)
    {
       while(x !=y )
       {
           if(x>y)
               x=x-y;
           else
               y=y-x;
       }
       return x;
    }
    public static void main(String args[])
    {
        int max,a=12,c=915;
       max= gcd(a,c);
        System.out.println(max);
    }
}
